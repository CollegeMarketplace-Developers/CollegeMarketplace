files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            * * * * * root cd /var/app/current && php artisan schedule:run >> /var/log/job1.log 2>&1
commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/mycron.bak"
